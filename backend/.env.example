# Required: PostgreSQL connection string
DATABASE_URL="postgresql://user:password@localhost:5432/budget_tracker"

# Required in production: JWT signing secret (generate with: openssl rand -base64 32)
# Defaults to a dev secret if unset - DO NOT use in production
JWT_SECRET="your-secret-at-least-32-chars"

# Optional: Port for the API server (default: 3000)
PORT=3000

# Optional: CORS allowed origins (comma-separated). Leave empty to allow all origins.
# Example: https://budget.example.com,https://www.budget.example.com
# CORS_ORIGIN=
