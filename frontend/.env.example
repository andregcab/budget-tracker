# Optional: API base URL for production builds when frontend and backend are on different origins.
# Omit to use relative /api (works when reverse proxy forwards /api to backend).
# Example: https://api.yourserver.local/api
# VITE_API_URL=
