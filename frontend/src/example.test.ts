import { describe, it, expect } from "vitest";

// Simple smoke test to ensure Vitest is wired correctly.
describe("example", () => {
  it("runs a basic assertion", () => {
    expect(true).toBe(true);
  });
});
